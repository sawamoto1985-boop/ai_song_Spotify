# Spotify AI Track Finder
Spotify APIを使ってAI楽曲の情報を取得するツールです。

## 使い方
1. `.env` ファイルを作成し、SpotifyのClient IDとSecretを設定してください。
2. `pip install -r requirements.txt` を実行します。
3. `python main.py` で実行します。
# ai_song_Spotify
ai_song_Spotify
